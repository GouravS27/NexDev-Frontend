<!-- Frontend NexDev  -->

1. Setting up React + Vite App + Tailwind + Daisy UI
2. Install React Router Dom [Create BrowserRouter -> Routes -> Route]
3. Install axios
4. CORS - install cors in backend => add middleware to with configurations: orgin, credentials: true
5. Install React-Redux-Toolkit [ configureStore => Provider => createSlice => add reducer to store ]
6. Add redux devtools in chrome
7.

<!-- App.jsx -->

1. Add Router
2. Wrapped with React Redux Store [ Provider with store name]
3. Store user info on Refresh [made api call /profile/view]

<!-- Login -->

1. Fetching User Details from /login API using axios and dispatching with addUser into Redux Store\
2. After Login Navbar Image should be updated [useSelector]
3. After Login , we should navigate to /feed [useNavigate]
4.
